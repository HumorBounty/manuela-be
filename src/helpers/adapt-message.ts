export const adaptSuccessMessage = (subject: string) => {
  return `${subject} added successfully.`;
};

export const adaptDeleteMessage = (subject: string) => {
  return `${subject} deleted successfully.`;
};

export const adaptEditMessage = (subject: string) => {
  return `${subject} edited successfully.`;
};

export const adaptFetchedMessage = (subject: string) => {
  return `${subject} fetched successfully.`;
};
